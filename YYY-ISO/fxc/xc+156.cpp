
/*
---------------------------------------
# ✅ xc+156 C++ Lib 11
# ✅ C++ criado: 2021/03/31
# ✅ Objetivo:sql server - C++ Lib 11
---------------------------------------*/

create proc [dbo].[xc+156]
AS
/*
#include <iostream>
#include <queue>
using namespace std;

int main()
{
    queue<int>fila1,fila2;

   fila1.push(10);
   fila1.push(20);
   fila1.push(30);
   fila1.push(40);
   fila1.push(50);
   fila1.emplace(60);

   cout << "primeiro elemento: " << fila1.front() << endl;
   cout << "ultimo elemento: " << fila1.back() << endl;
   cout << "tamanho da fila: " << fila1.size() << endl;

    fila2.swap(fila1);
    while(!fila1.empty()){
        cout << fila1.front() << " - ";
        fila1.pop();
    }
    if(fila1.empty())
{
    cout << "fila1 vazia" << endl;
}
      while(!fila2.empty()){
        cout << fila2.front() << " - ";
        fila2.pop();
    }
      if(fila2.empty())
{
    cout << "fila2 vazia" << endl;
}


    return 0;
}

*/