
/*
---------------------------------------
# ✅ xc+183 - C++ Lib 11
# ✅ C++ criado: 2021/04/14
# ✅ Objetivo:sql server - C++ Lib 11
---------------------------------------*/

create proc [dbo].[xc+183]
AS
/*
#include <iostream>

using namespace std;

int main()
{
    cout << " << Operador de insercao >> Operador de extracao" << endl;

    cout << "\nOperacao Ternaria - CONDICAO " << endl;
    cout << "\n<variavel> = (condicao) ? <valor1> : <valor2>\n " << endl;
loop:
    int y =0;
    cout << "Digite valor de y de 1/12: "  << endl;
    cin >> y;

    string mes =(y ==1) ? "Janeiro...":
                (y ==2) ? "Fevereiro...":
                (y ==3) ? "Marco...":
                (y ==4) ? "Abril...":
                (y ==5) ? "Maio...":
                (y ==6) ? "Junho...":
                (y ==7) ? "Julho...":
                (y ==8) ? "Agosto...":
                (y ==9) ? "Setembro...":
                (y ==10) ? "Outubro...":
                (y ==11) ? "Novembro...":
                (y ==12) ? "Dezembro...":
                     " Valor inexistente no intervalo 1/12";
    cout << mes << endl;

    cout << "Digite 99 para ternimar: "  << endl;
    int w =0;
    cin >> w;
    if(w !=99) {goto loop;
    }else{
        cout << "fim do programa\n\n";
    }

    system("pause");
    return 0;
}

*/