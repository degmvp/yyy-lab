
/*
---------------------------------------
# ✅ xc+138 - C++ Lib 11
# ✅ C++ criado: 2021/03/31
# ✅ Objetivo:sql server - C++ Lib 11
---------------------------------------*/

create proc [dbo].[xc+138]
AS
/*
#include <iostream>
#include <typeinfo>
#include <vector>

using namespace std;

int main()
{
    auto num{50};
    auto alf{'g'};
    auto fnum{1.45};
    vector<int> xnum={1,2,3,4,5,6,7,8,9};

    if(typeid(int).before(typeid(double))){
        cout << "int vem antes de double" << endl;
    }else{
        cout << "double vem antes de int"  << endl;
    }
    cout << "tipo da variavel: " << typeid(num).name() << endl;
    cout << "tipo da variavel: " << typeid(alf).name() << endl;
    cout << "tipo da variavel: " << typeid(fnum).name() << endl;
    cout << "tipo da variavel: " << typeid(xnum).name() << endl;
    return 0;
}

*/