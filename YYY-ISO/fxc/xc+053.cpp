/*
---------------------------------------
# ✅ xc+053
# ✅ C++ criado: 2021/03/05
# ✅ Objetivo:sql server - C++ Lib
---------------------------------------*/

create proc [dbo].[xc+053]
AS
/*

#include <iostream>

using namespace std;

int main()
{
    // Operador ternario

    int n1, n2, nota;
    string res;

    cout << "digite a primeira nota: ";
    cin >> n1;

    cout << "digite a segunda nota: ";
    cin >> n2;

    nota=n1+n2;

    // Se a nota for > 60 aprovado
    // Se a nota for < 60 reprovado

   //(nota >= 60) ? res="Aprovado" : res="reprovado";

   res=(nota >= 60) ? "Aprovado" : "reprovado";
    cout << "\n\nsituacao do aluno: " << res << "\n";

    int y1,x;
    y1=2100;
    cout << "\n\ndigite a faixa salarial: \n\n";
    cin >> x;

    (y1 <= x) ? y1=y1+600 : y1=y1+200;
    cout << "\n\nNovo valor do y1: " << y1 << "\n";






    return 0;
}

*/