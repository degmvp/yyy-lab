/*
---------------------------------------
# ✅ xc+215 C++ Lib 11
# ✅ C++ criado: 2021/04/30
# ✅ Objetivo:sql server - C++ Lib 11
---------------------------------------*/

create proc [dbo].[xc+215]
AS
/*
#include <iostream>
#include <memory>
#include <stdlib.h>
using namespace std;

int main()
{
    cout << "Matriz 5 x 3 linha coluna receba 1 caracter " << endl;
    cout << "Pesquisa Matriz 5 x 3 linha coluna " << endl;
    char matriz[5][3]; //l x c
    char alf;
    int l, c, flag=0;

    for(l=0; l<5; l++){
        for(c=0; c<3; c++) {
            printf("Matriz [%d, %d]: ", l+1, c+1);
            fflush(stdin);
            scanf("%c", &matriz[l][c]);
        }
    }
    printf("Caracter: ", l+1, c+1);
    fflush(stdin);
    scanf("%c", &alf);

    for(l=0; l<5; l++){
        for(c=0; c<3; c++) {
            if(matriz[l][c] == alf)
               flag=1;
        }
    }
    if (flag)
        printf("Encontrado\n");
    else
        printf("Inexistente!\n");

    system("pause");
	return 0;
}


*/